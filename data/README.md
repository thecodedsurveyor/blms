# Synthetic seed data

Phase 2 seed data is held in the API mock adapter so a fresh local run is deterministic without writing credentials to disk. Later phases will add generated synthetic NIN records, user metadata, mock-ledger snapshots, and sample properties here. No production identity or land data belongs in this directory.
